# 2026-09-26 — SCH-PATH

## Analysis only: what the partner merge actually integrated (no writes to git)

Badek reported "the merge did not go well, something that used to work is gone" and asked whether we can
revert to any commit. He then said **do not revert, analysis only** — he will say when. Later he asked for
the classmate's repo URL as proof of track.

### CORRECTION — my first pass measured against the wrong base

My first report said the classmate's branch was "an older baseline that deleted 25 of your files
(`api.ts`, `dateFormatter.ts`, `residentScope.js`, 25 test files)" and totalled 108 files / +3510/−13621.
**That was wrong.** I measured `82f05f7` (master *after* the 60 commits that followed the fork) against his
tip. Everything he "deleted" was actually work **Badek added after the fork point**, which his copy simply
predates. A stale fork makes your own later additions read as *his* deletions.

Correct base: **his own parent.** `e1cb775^` = `9883316` (badek, 2026-09-24 18:53:59) = the merge-base of
`82f05f7` and `e1cb775`. He has **exactly one commit** past that point; Badek has **60**.

| | measured vs `82f05f7` (WRONG) | measured vs `9883316` (RIGHT) |
|---|---|---|
| files | 108 | **48** |
| insertions | +3510 | **+3068** |
| deletions | −13621 | **−1311** |
| files deleted | 25 | **0** |

`git diff --name-status 9883316 e1cb775` → 4 A, 44 M, **0 D**. He deleted nothing.

### His real work: one commit, 48 files

`e1cb775` — `akosichano01-bot <akosichano01@gmail.com>`, 2026-09-25 19:13:54 +0800,
"Update SCH-PATH system 09/25/26". Biggest files (his numstat):

| file | + | − |
|---|---|---|
| `frontend/src/app/components/Health.tsx` | 364 | 29 |
| `frontend/src/app/components/CaseLoad.tsx` | 344 | 62 |
| `frontend/src/app/components/Tri.tsx` | 299 | 122 |
| `backend/src/controllers/dashboardController.js` | 221 | 0 |
| `frontend/src/app/components/Dashboard.tsx` | 175 | 171 |
| `backend/src/utils/triReportPdf.js` | 137 | 71 |
| `frontend/src/app/components/TriStatistics.tsx` | 122 | 0 |
| `backend/src/controllers/violationController.js` | 119 | 3 |
| `backend/src/controllers/assignmentController.js` | 112 | 85 |
| `backend/src/controllers/documentController.js` | 108 | 82 |
| `backend/src/controllers/childController.js` | 102 | 0 |
| `frontend/src/app/components/ChildRecords.tsx` | 96 | 89 |

### What landed, by 3-way classification of his 48 files

- **21 files — his change is in HEAD** (7 byte-identical to his version; 14 re-applied on top of Badek's
  newer base). Includes `Tri.tsx`, `TriStatistics.tsx`, `abscond.js`, `dashboardController.js`,
  `dashboardRoutes.js`, `childController.js`, `server.js`, `schema.sql`, `ChildDetail.tsx`,
  `ChildRecords.tsx`, `InterventionTracker.tsx`, `Notifications.tsx`, `PhaseProgress.tsx`, `DataContext.tsx`.
- **27 files — his change is NOT in HEAD** (HEAD equals either the fork point or Badek's pre-merge master).
  Notable: **`Health.tsx` (+364), `CaseLoad.tsx` (+344), `Dashboard.tsx` (+175/−171), `Violations.tsx`
  (+83), `triReportPdf.js` (+137/−71), `violationController.js` (+119), `assignmentController.js` (+112),
  `documentController.js` (+108), `triController.js` (+88), `healthRecordPdf.js` (+46),
  `residentScope.js` (+5/−63)**, plus 2 `rbac.definition.json` copies and 6 test files.

So the two integration passes (`a04eedb` "conservative merge pass 1", `9e17d02` "integrate partner
absconded feature; drop inert residue") kept **21 of his 48 files** and reverted **27**. The discard was
defensible for the stale-fork files but collateral for the additions — **his Health/CaseLoad/Dashboard/
Violations UI work is not in production.**

### Nothing is unrecoverable

`e1cb775` **is an ancestor of HEAD** (it is the second parent of `73bfe96`, which is on `origin/master`),
and the fork point `9883316` is an ancestor of master too. So his whole branch is reachable from any clone
via `73bfe96^2`; it just has no branch name. Both `backup/pre-partner-merge` (`82f05f7`) and
`merge/partner-2026-09-25` (`6dcd11b`) are **local-only** (origin has `master` + `refs/pull/1/head` =
`63d5427`, an object not present locally).

### Where his repo is (asked for as proof)

- Only remote configured in this working copy: `https://github.com/bdknatividad/SCH-PATH.git` (Badek's).
- His GitHub account is `akosichano01-bot` (display name "inchan"). Public repos are `inchan`,
  `tenant-system`, `EcomsFinals`, `finals-` — **no public SCH-PATH**. So there is no repo URL to hand over.
- The proof of track is the commit itself: `e1cb775`, parent `9883316`, 48 files, in this repo's history.
  Can be handed over as a git bundle (`git bundle create … 9883316..e1cb775`) or pushed as a named branch.

### Live state at time of analysis

Vercel serving `assets/index-D9QvwdHN.js`, `Last-Modified: 2026-09-25 15:48:43Z` — the merged frontend
**is** live; Railway `/api/health` 200. `ab3fc51` touched backend only, so the frontend hash is unchanged.

### Only 8 frontend files differ from pre-merge master

`ChildDetail.tsx`, `ChildRecords.tsx`, `InterventionTracker.tsx`, `Notifications.tsx`, `PhaseProgress.tsx`,
`Tri.tsx` (+6), `TriStatistics.tsx` (new), `DataContext.tsx`. If a *regression* is what he is seeing, it is
in one of those eight or in the KEPT backend files.

## Deliverable produced: the re-integration prompt

Badek asked for a prompt that would let a cold session completely merge both versions. Written to
**`.workbuddy-ai/prompts/merge-partner-version.md`** — self-contained: carries the SHAs, the fork-point
trap, the 21/27 split with his numstat, the per-file 3-way method, the repo invariants (timezone contract,
`errorHandler` masking, `residentScope` forward-only scoping, duplicated `rbac.definition.json`), the
local verification baseline (6 environmental failures, 9 known `tsc` errors, `lf-fix.py`), the live
verification protocol (probe traps, no `admin` account, read-only), and the ship-and-confirm steps.

Also recorded the honest caveat: `residentScope.js` (+5/−63), `assignmentController.js` (+112/−85),
`Dashboard.tsx` (+175/−171) and the two `rbac.definition.json` copies are the likely **real
contradictions** — both sides changed the same logic — and need a deliberate decision rather than a
mechanical re-apply.

## HIS REPOSITORY — verified (this corrects an earlier wrong answer)

**`https://github.com/akosichano01-bot/SCH-PATH-MY-VERSION`**, branch `master`.

Source of truth: **`.git/FETCH_HEAD`**, written by git at fetch time and never cleaned here —
`e1cb775…<TAB><TAB>branch 'master' of https://github.com/akosichano01-bot/SCH-PATH-MY-VERSION`.
Backed up to `/tmp/FETCH_HEAD.evidence.bak` before re-fetching.

Confirmed reachable: unauthenticated `git ls-remote https://github.com/akosichano01-bot/SCH-PATH-MY-VERSION`
→ exit 0, so the repo is **public**.

**Earlier I told Badek "no public SCH-PATH, so there is no repo URL to hand over." That was wrong.** I
looked at his GitHub *profile* page and searched for a repo named `SCH-PATH`; his is
`SCH-PATH-MY-VERSION`, so it never matched. The answer was in `.git/FETCH_HEAD` the whole time. Lesson:
**a fetch leaves its origin URL in `.git/FETCH_HEAD` — read it before concluding a remote is unknown.**

## ⚠️ His repo has MOVED — our snapshot is stale

| | sha | when |
|---|---|---|
| what we merged | `e1cb775` | 2026-09-25 19:13:54 |
| **his master now** | **`b927b8b`** | 15 commits later |

All 15 new commits are titled **"Add files via upload"** (GitHub web-UI uploads), and `b927b8b` **is a
descendant of `e1cb775`** — his history was not rewritten, his new work sits on top.

The 15 new commits: 11 files, **+4823 / −3**:

| file | + | − |
|---|---|---|
| `backend/src/database/expectedSchema.json` | 4504 | 0 |
| `backend/src/utils/schemaSync.js` | 112 | 0 |
| `backend/src/middleware/errorHandler.js` | 54 | 1 |
| `backend/src/controllers/violationController.js` | 40 | 0 |
| `frontend/src/polyfills.ts` | 34 | 0 |
| `frontend/src/routes/index.js` → `backend/src/routes/index.js` | 30 | 1 |
| `frontend/src/app/utils/lazyComponent.tsx` | 27 | 0 |
| `backend/src/server.js` | 15 | 0 |
| `frontend/src/app/App.tsx` | 7 | 0 |
| `frontend/public/pdf.worker.mjs` | 3 | 0 |
| `frontend/src/main.tsx` | 2 | 0 |

**His total work is now 56 files, +7891 / −1314** (was 48 / +3068 / −1311).

### Re-classified against `b927b8b` (the correct target)

| bucket | n | meaning |
|---|---|---|
| A | **22** | we never touched it post-fork, his version not in HEAD → take his, no contradiction risk |
| B | 0 | — |
| C | **27** | we also changed it, his version not in HEAD → his wins where regions overlap |
| D | 7 | both already in HEAD |

Bucket A gained 8 of the new files: `expectedSchema.json`, `schemaSync.js`, `errorHandler.js`,
`pdf.worker.mjs`, `App.tsx`, `lazyComponent.tsx`, `main.tsx`, `polyfills.ts`.

### Dry-run of the real merge now shows ONE conflict

`git merge-tree --write-tree --name-only HEAD b927b8b` → **exit 1**,
**`backend/src/controllers/violationController.js`** conflicts. `backend/src/routes/index.js` and
`backend/src/server.js` auto-merge. Under Badek's rule the conflicting file resolves to **his**.
(Against the stale `e1cb775` the same dry-run was clean — the new commits are what introduce it.)

### Three cautions on his new work — review, do not blind-take

1. `expectedSchema.json` (4504 lines) + `schemaSync.js` — a schema-drift system. This repo's rule is that
   **`schema.sql` is never executed** and only boot migrations + lazy `CREATE TABLE`s shape a deployed DB.
   His system may assume otherwise; check what calls it.
2. `backend/src/middleware/errorHandler.js` (+54/−1) — this is the file that **masks every MySQL error**
   (`ER_*` → "Database error occurred"). Any change here alters every failure path.
3. `frontend/public/pdf.worker.mjs` (+3) — a hand edit to a **vendored** pdfjs file. Repo rule: never
   `immutable`, always `?v=${pdfjs.version}`. Confirm it is not a version mismatch with bundled pdfjs-dist.

## VERIFIED FEATURE INVENTORY — what merging his version actually adds

Read from his code (`9883316 → b927b8b`), not inferred. Mapped to Badek's 51-item revision list where it
matches — **his branch independently implements several items on that list.**

### User-facing modules

1. **Dual verification of logged incidents** — new `violations` columns `psychVerifiedBy`, `psychVerifiedAt`,
   `psychVerification` (LONGTEXT), `swVerifiedBy`, `swVerifiedAt`; helper `ensureViolationVerificationColumns()`
   and `resolveViolationVerificationSide()`. New endpoints `GET /api/violations/:id/review-preview` and
   `POST /api/violations/:id/review`. `Violations.tsx` gains "Verification (both required)",
   "Verify as: Psychological Support Staff / Social Worker", a "Pending" queue and the empty state
   "No logged incidents awaiting verification." → **revision item 22**.
2. **Houseparents can log violations** — `POST /api/violations` now `authorize('socialworker','centerhead',
   'houseparent')`, and the `houseparent` role gains `Violations: create`. → **revision item 37**.
3. **"Scheduled Today" fixed** — new `GET /dashboard/schedule-summary` (`dashboardController.scheduleSummary`)
   and a Dashboard UI with *Total schedules*, *Assigned Schedules*, and open Activities / Assessments /
   Court Hearings / assigned-intervention schedules, sourced "from the database". → **revision item 6**.
4. **Nurse / Health module** — `Health.tsx` (+364) and `healthRecordPdf.js` (+46): auto **BMI**, **monthly**
   height/weight records, **Laboratory Results**, **Medical Certificate**, **Classification**, Download.
   → **revision items 17 and 18**.
5. **Caseload Manager** — `canAssignCaseLoadManager()`, `caseloadScope`, `activeHouseparents`, a reworked
   `CaseLoad.tsx` ("Every resident already has a Houseparent.", "No residents assigned"), and
   `assignmentController.js` (+112/−85). → **revision items 1 and 2**.
6. **TRI signatories rework** — `Tri.tsx` (+299/−122), `triReportPdf.js` (+137/−71), `triLayout.json`:
   a `TRI_SIGNATORIES` slot table, `drawTriSignatures()` with per-slot geometry, PNG/JPEG embedding via
   `pdf.embedPng`/`embedJpg`, and a `TriNameField` that commits on blur/Enter. **Overlaps our item 8 work —
   this is bucket C, the one to reconcile most carefully.**
7. **Phase writes blocked for absconded residents** — `blockAbscondedPhaseWrites` on phase
   create/update/validate/complete/demote/return/task; `POST /api/children/:id/abscond` gated by
   `requirePermission('Child Records','edit')`. (`abscond.js` already merged.)
8. **Evaluation module gating** — `authorizeNonHouseparent` on evaluation create/update/delete.
9. **Documents refuse absconded residents** — `assertResidentNotAbsconded(data.residentId, 'given new
   documents')` on document create.

### Platform / infrastructure

10. **Schema self-repair** — new `backend/src/database/expectedSchema.json` (4504 lines) +
    `backend/src/utils/schemaSync.js`: on boot and on demand, compare the live DB with what the code uses,
    **add every missing column as NULL-able**, and **widen an ENUM** that lacks values the code writes.
    Nothing is dropped, renamed or narrowed; each change is independent. New `GET /api/health/schema`
    (`authenticate` + `authorize('centerhead','admin')`) reports drift; `?fix=1` repairs now. Well-designed
    and additive — the honest read is that this is a genuine improvement over our hand-written boot
    migrations.
11. **Error-handler self-repair** — `errorHandler.js` (+54/−1) catches `ER_BAD_FIELD_ERROR`,
    `ER_NO_SUCH_TABLE`, `WARN_DATA_TRUNCATED`, `ER_TRUNCATED_WRONG_VALUE_FOR_FIELD`, runs the schema sync
    (rate-limited to once per minute), then answers **409** with "The database was missing X and has just
    been updated. Please try again." Note: this changes the failure contract of **every** request — review.
12. **Stale-chunk recovery** — `lazyComponent.tsx` detects a failed dynamic import
    (`isChunkLoadError`) after a new deploy and reloads **once** (guarded by `sessionStorage`
    `sch-path:chunk-reload`) instead of showing the error page. Directly fixes the "tab open across a
    deploy" breakage.
13. **PDF viewer polyfills** — new `frontend/src/polyfills.ts` (imported first in `main.tsx`) plus the same
    two polyfills at the top of `public/pdf.worker.mjs`, for `URL.parse` and `Promise.withResolvers`
    (needed by pdf.js on browsers older than Chrome/Edge/Firefox 126). Fixes "URL.parse is not a function".
14. **Error page shows the real message** — `App.tsx` error boundary renders `error.message` (300 chars) so
    a screenshot diagnoses it.
15. **`violations`/resident status ENUM gains `Absconded`** — `status ENUM('Active','Discharged','Absconded')`.
16. **`applyIncidentReportDecision()` restored** — a function that "had been deleted while its export was
    left in place" (a latent 500, exactly the class of bug our notes warn about). Now owns its transaction.

### Behaviour changes that are NOT features — the risk surface

- **`residentScope.js`** deletes the 63-line legacy fallback; only an explicit `residentAssignments` row
  puts a resident on a caseload.
- **Boot migration ends automatic caseload assignments** — `server.js` sets `status='Ended'` for
  `residentAssignments` rows with source `system` / `admission` / `admission-update` (idempotent; manual
  `caseload`/`manual` rows untouched). **This writes to live data at boot.**
- **`houseparent` RBAC change** — `Violations: view→view,create`; `Activities: view,create,edit,export →
  view,export`.

⚠️ **Highest-risk combination to verify live:** #6 + the two bullets above together mean a resident whose
only link to a Houseparent was the Admission Slip's `houseparentOnDuty` will lose that Houseparent at the
same boot that ends the automatic assignments. Check `/resident-assignments/my-residents` and `/store`
for a Houseparent immediately after deploying, against the pre-deploy list.

## FORMS — per-form reconciliation, verified (Badek asked specifically)

Checked `git diff --numstat 9883316 b927b8b` (his) against `git diff --numstat 9883316 HEAD` (ours) per
form file. **He did not touch every form.**

| form | his | ours | verdict |
|---|---|---|---|
| **TRI** — `Tri.tsx`, `triReportPdf.js`, `triLayout.json`, `triController.js`, `triRoutes.js` | +299/−122, +137/−71, +12/−4, +88/−6, +5/−0 | **+341/−35, +234/−60, +35/−4, +177/−2, +7/−0** | ⚠️ design fork |
| **Incident Log** — `IncidentReportModal.tsx`, `incidentReportController.js` | +20/−5, +50/−13 | +7/−2, **nothing** | take his, unopposed |
| **Anecdotal Report** — `AnecdotalReport.tsx`, `AnecdotalReports.tsx`, `anecdotalReportPdf.js` | **nothing** | +13/−7, +14/−15, +43/−7 | ours only |
| **Quarterly Progress Report** | **nothing** | +55/−20 | ours only |
| **Health / Medical** — `Health.tsx`, `healthRecordPdf.js` | +364/−29, +46/−9 | nothing | take his |
| **Admission Slip** — `admissionController.js` | +39/−5 | present | per the rule |
| **Violations** — `Violations.tsx`, `violationController.js`, `violationGuideController.js`, `violationRoutes.js` | +83/−14, +159/−3, +3/−1, +7/−3 | `Violations.tsx` both | take his |

**He never touched the Anecdotal Report or the Quarterly Progress Report** — those differences are entirely
our post-fork work, so they need no reconciliation. (My earlier wrong-base measurement made them look like
his, because our own changes read as his under the stale-fork artifact.)

### ⚠️ The TRI page-8 signature block is a DESIGN FORK, not a merge

Both sides built the same feature in the same files, with **incompatible storage**:

- **His:** a single `signatories` JSON column on `triRecords` (`UPDATE triRecords SET signatories = ?`,
  read via `signatoriesOf()` / `asObject(row.signatories)`), geometry from `triLayout.signatories`; plus the
  older `houseparentSignature`/`houseparentSignedBy`/`houseparentSignedAt` columns.
- **Ours:** **15 individual columns** (`centerhead*`, `sectionchief*`, `adminOfficer*`, `swo1*`, …), live,
  with **TRI006's stored signature drawings already in them**.

`git merge-tree` says these files **auto-merge cleanly** — the dangerous case: a clean text merge of two
incompatible designs yields a file that saves and renders neither correctly.

Decision needed before any TRI code: (1) which storage wins; (2) if his, migrate the live column data into
`signatories` first or it is orphaned; (3) preserve our item-8 rendering rules (four signable lines, printed
name **below** the signature on the Houseparent line, every signature **centred over the printed name**, not
over its box); (4) update `tri-designated-signatures.test.js`, `tri-houseparent-signature.test.js` and
`anecdotal-report-pdf.test.js` deliberately, never delete them.

This is the same fork the 2026-09-25 session logged as "13 conflicts, the TRI signature design fork" — it
was identified then and deferred. It is now the blocking item.

## IMPLEMENTATION — progress (2026-09-26, branch `merge/partner-2026-09-26`)

Badek's decisions: **his TRI wins**; then "implement all that now to our deployment".

### Done

- **Phase 0** — pushed his branch and the safety refs to origin, verified with `git ls-remote`:
  `partner/akosichano01-2026-09-26` = `b927b8b`, `partner/akosichano01-2026-09-25` = `e1cb775`,
  `backup/pre-partner-merge` = `82f05f7`, `merge/partner-2026-09-25` = `6dcd11b`. `master` untouched.
- **Phase 1** — scratch branch `merge/partner-2026-09-26`; merge of `b927b8b` committed as **`1132590`**
  (parents `24d6fa6` + `b927b8b`). One conflict, `backend/src/controllers/violationController.js`,
  resolved to his (our delta on that file is empty). No markers anywhere.
- **Pre-deploy baseline** captured to `C:/tmp/baseline-caseload.json` (+ `/c/tmp/asg-CH00*.json`).
- **Re-integration** (`/c/tmp/reintegrate2.sh`, fork point `9883316` as base): 56 files → 30 already his,
  1 taken outright, 25 three-way merged, 0 markers left. Bucket-A wins verified present: `Health.tsx`,
  `CaseLoad.tsx`, `residentScope.js`, `healthRecordPdf.js`, `errorHandler.js`.

### ⚠️ THE TRAP THAT MAKES A PLAIN MERGE USELESS (most important thing this session)

`git merge-base 24d6fa6 b927b8b` = **`e1cb775`**, his *old* tip — NOT the fork point, because our master
already contains `e1cb775` from the first merge. So a plain `git merge` reads the `a04eedb`/`9e17d02`
**reverts as our changes**, and keeps them. It "succeeds" and still loses his work — it brought in only the
11 files of his 15 new commits. **Re-integrating requires the fork point as the merge base, per file**
(`git merge-file -p --theirs ours base theirs`), which is what `reintegrate2.sh` does.

### Still to do

1. **TRI (3 files)** — `Tri.tsx`, `triReportPdf.js`, `triController.js` still carry our merged regions.
   Badek chose **his TRI**, so these must be taken wholesale from `b927b8b`, plus a guarded migration
   copying the live per-column signature data into his `signatories` JSON so TRI006's drawings are not
   orphaned. (`triLayout.json`, `triRoutes.js` already == his version.)
2. **Phase 3** audit, **Phase 4** suite + build, **Phase 5** deploy.
3. **BLOCKER — the caseload migration.** See below. Must be resolved before pushing.

### 🛑 BLOCKER: his boot migration empties every Houseparent's caseload

`server.js` (his) ends `residentAssignments` rows with `status='Active' AND assignmentType='houseparent'
AND source IN ('system','admission','admission-update')`. Live data (captured 2026-09-26 08:35 +08:00):

| resident | assignment | type | source | createdBy | outcome |
|---|---|---|---|---|---|
| CH001 | ASN037 | houseparent | `admission` | centerhead | **ended** |
| CH002 | ASN013 | household | `system` | system | survives (not houseparent) |
| CH003 | ASN025 | houseparent | `system` | system | **ended** |
| CH004 | ASN038 | houseparent | `system` | system | **ended** |
| CH005 | ASN048 | houseparent | `admission` | centerhead | **ended** |
| CH006 | ASN049 | houseparent | `admission` | centerhead | **ended** |

**5 of 6 residents lose their Houseparent.** His comment claims those rows came from "the old Admission
Slip code … neither writer exists any more", but three were created by `centerhead` at 15:44, 15:51 and
23:45 on 09-25 — through the current UI. So the premise is false for this data. Recommend narrowing the
WHERE to genuinely automatic rows (e.g. `createdBy IS NULL OR LOWER(createdBy) IN ('system','seed')`)
before shipping. **Awaiting Badek's call.**

Good news: `houseparentOnDuty` / `houseparentUserId` are **absent keys** on every `/children` row, so those
columns do not exist in the deployed DB — the legacy fallback his `residentScope.js` deletes was already
dead code. That half is safe.

### The document in his screenshot = Form 08, the Incident Report

Identified by string match, not guesswork: every string in the block (`Reported by`, `Endorsed to`,
`Checked by`, `Noted by`, `Signature over Printed Name`, `Joyce Anne`, `Tenorio`,
`Francis C. Patricio`) lives in **`backend/src/controllers/incidentReportController.js`** — the digital
**Form 08 (Second Chance Home Incident Report)**. His version:
- **adds a Psychological Support Staff sign-off** — `FORM08_PSYCH_STAFF_NAME = 'Joyce Anne D.C. Tenorio'`,
  role `Psychological Support Staff`, signature box `{ x: 36, top: 804, width: 140, height: 30 }` below
  "SWO I - Case Manager", with printed name, rule, then role; new `psychStaffSignature` column threaded
  through the INSERT and UPDATE.
- **corrects two printed names we had wrong**: `FORM08_ENDORSED_TO_NAME` `"Ma'am Joyce"` → `''` (now
  fillable; the typed `endorsedTo` prints) and `FORM08_NOTED_BY_NAME` `'Sir Francis'` →
  `'MARICOR C. NAVARRO, RSW'` / `SWO II - Center Head`.
- fixes a stuck-report bug: a report whose document is `Rejected` while the incident still reads
  `Submitted` could not be re-filled; it now consults the linked document's status too.
- `psychStaffSignature` **is** declared in `schema.sql` and in the guarded boot migration at
  `server.js:2199`. ✔

Note this **supersedes revision-list item 23** as recorded ("Ma'am Joyce at the top, Sir Francis at the
bottom") — the screenshot Badek endorsed shows Joyce as Psychological Support Staff and Maricor Navarro as
Noted by. Correct the revision list next time it is edited.

### TRI — the compatibility layer (Badek: "his TRI wins" + "without losing functions")

Both sides built the same five-line page-8 block with different storage, so "his wins" alone would have
lost our work. Reconciled instead:

- **His slots are the same five lines as our columns** — `houseparent`, `administrativeOfficer`,
  `caseManager`, `centerHead`, `sectionChief` ↔ our `houseparent*`, `adminOfficer*`, `swo1*`,
  `centerhead*`, `sectionchief*`.
- **His design REQUIRES the columns**: `signatorySignatureOf()` reads the Houseparent E-Signature from
  `record.houseparentSignature` (the other four from the `signatories` JSON). So the column migration is
  load-bearing, not legacy — and the merged `server.js` had lost it.
- **His storage**: `triRecords.signatories` JSON, `{ slot: { name, signature, signedBy, signedAt } }`.
  Houseparent line = name in the JSON, signature in the columns.

Implemented in `backend/src/controllers/triController.js`:
- `SIGNATORY_COLUMNS` — slot → column triple; `slotForSignatureLine()` derives a slot from the line's own
  column names, so the table stays the single source of truth.
- `mirrorSignatoryToColumns()` — copies a slot's E-Signature into that line's columns, only for fields the
  entry actually carries (never blanks a column it was not told about), tolerating errno 1054/1060.
- `updateSignatories()` (his) now mirrors after writing the JSON.
- `signOfficialLine()` (ours) **restored** — it now writes the JSON slot *and* mirrors the columns, so the
  old `POST /:id/signature/:line` route keeps working with identical storage.
- Exports restored: `signOfficialLine`, `publishMissingTriDocuments`, `OFFICIAL_SIGNATURE_LINES`.
  Note `OFFICIAL_SIGNATURE_LINES` **survived** the merge on its own — my first attempt redeclared it and
  `node --check` caught the duplicate. Always syntax-check after inserting near a merged region.
- `triRoutes.js`: restored `POST /:id/signature/:line` → `signOfficialLine`.
- `server.js`: restored the 15-column guarded `ALTER` loop in the `triRecords` block, added our 12 columns
  to the `CREATE TABLE`, and added an **idempotent backfill** copying any column-only signature into its
  `signatories` slot (never overwriting a slot that already holds one).

Verified: `node --check` clean on all four files; the controller loads and exports both sides' functions.
His `triLayout.json` already satisfies our item-8 rendering rules — `houseparent.nameY` 775 sits *below*
`signatureY` 784, i.e. printed name below the signature. The printed names moved out of
`triLayout.designatedPersonnel` (now absent) into `TRI_SIGNATORY_SLOTS` in `utils/triReportPdf.js`; the
comment in `triController.js` that still points at `designatedPersonnel` is now stale and should be fixed.

## Suite result after the merge: 80 failures vs a baseline of 6 — NOT SHIPPABLE

`node --test "tests/**/*.test.js"` → 1301 tests, 1221 pass, **80 fail**. Baseline at HEAD is 6
(5 `jwt-secret`, 1 dialog-guard). Log at `C:/tmp/suite-after.txt`.

Failure clusters and their nature:
- **Text-pin tests asserting our old behaviour that his version deliberately changes** — e.g.
  `Form 08 pre-prints the endorsement and the noted-by name` ("the endorsement line is not pre-printed"),
  `the record keeps the printed name`, `the modal shows the same names the PDF stamps`, `the endorsement
  line is no longer typed` — these assert `"Ma'am Joyce"` / `'Sir Francis'`, which his version replaces
  with a fillable endorsement and `MARICOR C. NAVARRO, RSW`. **The tests are now wrong, not the code.**
- **Pins on frontend components his version replaced** — `Dashboard.tsx no longer renders the stored
  hearing type verbatim`, `assessments-column-widths`, `every Form 08 signature box is measured against the
  real template` (his new `psychStaff` box changes the measurements).
- **Possibly real regressions** — `the Case Load screen warns about residents a month without a manager`
  (+ the exclusion case), `BMI is derived from the recorded height and weight, never typed`,
  `the medical sheet logs one consultation per row`, `only the Center Head can assign or change a Case Load
  Manager`, `a role without the capability is refused`, `manilaToday` import checks,
  `the app-level error screen does not print the exception` (his `App.tsx` deliberately prints it — a real
  behaviour conflict), body-markings cluster, `official-guide-seed-idempotence`.
- The 6 known environmental ones are inside the 80.

**Each of the 80 must be classified as "test needs updating to the new truth" vs "real regression" before
anything ships.** Not started.

## Still outstanding

1. Classify and fix the 80 failures.
2. **The caseload migration decision is still unanswered** — as written it ends 5 of 6 live assignments.
3. `vite build`.
4. Deploy + live verification.

## Tooling lessons

- **Never pipe a mutating script to `head`.** `bash script.sh | head -80` gave the script SIGPIPE partway
  through the file list; it stopped silently after 28 of 56 files. Redirect to a file instead.
- Bash's `/tmp` is not python's `/tmp` (python resolves it to `C:\tmp`). Use explicit `/c/tmp/...` and
  `C:/tmp/...` on both sides.
- Python sockets are sandbox-blocked here (`ssl` handshake fails); use `curl` for HTTP and python only for
  parsing files.

## Tooling note added

`git diff --quiet <a> <b> -- <path>` where the path exists in neither commit exits 0 (no diff), which
reads as "identical". Guard any such classifier with an explicit `git cat-file -e <rev>:<path>` presence
check, or a file absent on both sides silently lands in the wrong bucket.

## Badek's resolution rule for the re-integration (stated 2026-09-26)

> **Same part changed by both sides → take HIS. Only we changed it → keep OURS.**

Checked against the actual merge: `git merge-tree --write-tree --name-only HEAD e1cb775` returns a tree
hash and **no conflict list** — merging his branch into HEAD is textually conflict-free. So the rule
reduces to *take every one of his changes, keep everything of ours he did not touch*, and no hunk-level
adjudication is needed.

His 48 files re-classified by *(we changed it after the fork?)* × *(his version is what HEAD has?)*:

| bucket | n | meaning |
|---|---|---|
| A | 14 | we never touched it post-fork, his version not in HEAD → take his wholesale, zero risk |
| B | 0 | — |
| C | 27 | we also changed it, his version not in HEAD → his wins where regions overlap |
| D | 7 | both already in HEAD |

**The two places "his wins" has a real effect** (both verified by reading his delta):

1. `backend/src/utils/residentScope.js` — his version **deletes the 63-line legacy fallback** that put a
   resident on an HP's caseload by matching the Admission Slip's `houseparentOnDuty`, replacing it with a
   comment that only an explicit `residentAssignments` row puts a resident on a caseload. This is exactly
   **revision-list item 1** (HP on Duty ≠ Case Load Manager), so it is wanted. Live consequence to check:
   any resident linked *only* by `houseparentOnDuty` drops off that HP's list.
2. `backend/src/config/rbac.definition.json` + `frontend/src/app/config/rbac.definition.json` (currently
   byte-identical — keep them so) — his 2-line edit changes the **`houseparent`** role:
   `Violations: [view]` → `[view, create]`, `Activities: [view, create, edit, export]` → `[view, export]`.
   That is revision items **37** and **45**. Silent-revocation risk: empty `accessibleModules` falls back
   to the role matrix, so a wrong edit revokes access with no error.

Prompt file updated with the rule, the bucket map and both flags.

## Tooling notes (both cost a wrong reading this session)

- **Never measure a fork's diff against your own later master.** `git merge-base <your-master> <fork>` is
  the only correct base; otherwise your own post-fork commits read as the fork's deletions. This is the
  single biggest trap in this repo's merge work.
- `git diff --numstat <a> <b> -- <paths>` with an empty path list silently falls back to the whole range.
- To classify a fork's files: `git diff --quiet <fork> HEAD -- <f>` (absent), `git diff --quiet <pre> HEAD
  -- <f>` (present-but-differs), `git diff --quiet <fork-tip> HEAD -- <f>` (byte-identical).

## REVERT (08:53) — user halted the merge work

Badek: *"revert all you made you are getting out of hands."* Reverted the working tree and branch
pointer back to pre-session state. Nothing lost.

- `master` is now `24d6fa6` — the exact pre-merge commit, working tree clean (`git diff HEAD` empty).
- The re-integration + TRI compat work is preserved on `merge/partner-2026-09-26` @ **`71f1bc9`**
  ("WIP snapshot: partner re-integration + TRI compat layer"), unpushed. Branch NOT deleted.
- Before reverting I found and fixed one real blocker: the 3-way merge had **duplicated
  `seedDatabase()`** in `backend/src/scripts/seedDatabase.js` — his `try {`-wrapped body spliced into
  our `seedDefaultUsers()` for-loop, leaving the `for` unclosed → `SyntaxError: Unexpected end of
  input` at EOF. That one file broke every test that `require`s it (~52 of the 80 failures).
  The fix is in `71f1bc9`, not in master.
- Two untracked doc files left in place: `.workbuddy-ai/memory/2026-09-26.md`, `.workbuddy-ai/prompts/`.

## Verified: the exact live blast radius of his boot migration

Exact predicate (`b927b8b:backend/src/server.js:1167`):
`UPDATE residentAssignments SET status='Ended', endAt=COALESCE(endAt,NOW()), updatedBy='system-cleanup'
 WHERE status='Active' AND LOWER(TRIM(assignmentType))='houseparent' AND source IN ('system','admission','admission-update')`

Measured against the live snapshot (`C:/tmp/asg-CH00*.json`, 49 Active rows / 6 residents):

| | |
|---|---|
| rows the migration ENDS | **28** |
| residents affected | **all 6** (CH001–CH006) |
| Active houseparent-type rows left after boot | **0** |
| rows that survive | 21 × `household`/`system` — but only for CH001 and CH002 |

So CH003–CH006 keep nothing at all; CH001/CH002 keep only `household` rows, and whether those
still put the resident on a caseload depends on `residentScope.js` — **not verified**. Earlier
"5 of 6" was wrong: the predicate hits every resident.

**This is a live-data write at boot. A code rollback does NOT undo it** — the rows are already
`Ended` with an `endAt`. That asymmetry is the single strongest reason to gate the deploy.

## Revised the merge prompt to be ship-safe (08:57)

Rewrote `.workbuddy-ai/prompts/merge-partner-version.md` (350 lines). It now leads with a hard safety
contract instead of "ship it to production". What changed:

- **Push is gated behind explicit human approval.** The old text said "Ship it to production" /
  "Push to master"; Badek's rule is no pushes without asking.
- **Phase 0 no longer pushes backup refs** to GitHub.
- **§3.1 is a blocking human decision**, with the measured blast radius (28 rows / all 6 residents).
- **§2 says do not redo the merge** — resume from `71f1bc9`, which already holds the whole
  re-integration. Re-deriving it would re-introduce the duplication bug.
- **§3.2 adds the `node --check` gate** and the duplication trap that caused it.
- **§4 records the decisions already taken** (TRI = his, Form 08 = his, Anecdotal/QPR = ours) so a
  fresh session does not re-litigate the TRI design fork.
- **§5 replaces the old "27 files to re-integrate" table with the real 77 failures**, attributed per
  file, each marked update-the-test vs investigate vs real-regression.
- **§8 adds a rollback plan** that states plainly that the live-data write is the one thing a deploy
  revert cannot undo.

### Verified numbers now in the prompt (re-measured, not assumed)

| | |
|---|---|
| `71f1bc9` vs `master` | 47 files |
| `node --check` on changed `.js`/`.mjs` at `71f1bc9` | 27 checked, **0 failures** |
| suite at `71f1bc9` | 1323 tests, 1240 pass, **83 fail** (baseline 6 → **77 real**) |
| largest cluster | TRI signature — 33 of 77 across 4 test files |

Earlier "80 failures" was measured *before* the `seedDatabase.js` fix and is superseded.

### Two tooling traps worth remembering

- **`node --test` TAP gives file attribution via the `location:` field** inside each `not ok` record
  (`location: 'C://...//tests//foo.test.js:172:1'`). The `not ok` line itself carries only the test
  *name*. Parsing names instead of locations mis-attributes failures.
- **`node --check <file>` infers module type from the extension.** Writing every file to `_chk.tmp`
  made all 27 files report as syntax failures. Must write to a real `.js` / `.mjs` name.

## PHASE A RE-RUN (09:02, session resumed from the ship-safe prompt) — 76 failures, not 83

Resumed at `merge/partner-2026-09-26` = `71f1bc9`, working tree clean, 47 files vs `master`. All confirmed.

| gate | expected by the prompt | measured |
|---|---|---|
| files vs `master` | 47 | **47** ✔ |
| `node --check` on changed `.js`/`.mjs` | 0 failures | **27 checked, 0 failures** ✔ |
| suite | 1323 / 1240 pass / **83 fail** | 1323 / 1247 pass / **76 fail** ✘ |

**The 83 was a CRLF artifact, and I proved it rather than assuming it.** The missing 7 are exactly
`health-document-publish` (4→0), `admission-guardian-and-status` (2→0), `admission-body-markings` (8→7).
All three read source files as **text** and match multi-line markers containing `\n`
(`/\n\s+admissionStatus,\n\s+expectedDischargeDate,/`, `indexOf('{formStep ===\n              2 && (')`).
Decisive experiment: converted the whole tracked tree to CRLF → those three files gave **14** failures;
back to LF → **7**. Delta = 7 = the exact discrepancy. So the prompt's 83 was measured on an
un-normalised (CRLF) tree, and the true code-level count is **76 = 6 environmental + 70 real**.
Run `lf-fix.py` *before* measuring the suite, not after.

### §3.1 — the decision is not what the prompt describes (three corrections)

**(1) The seed used to re-create the assignments, and `71f1bc9` stops it.** Boot order in `startServer()`
is `testConnection → runMigrations() [the UPDATE] → syncSchema → publishMissingTriDocuments →
seedDatabase() → app.listen`. On **live master** `seedDatabase()` calls `seedResidentAssignments()`
(`runSeedStep('resident assignments', …)`), which assigns the **full Active-houseparent × Active-child
cross product** with `assignmentType='houseparent', source='system'`. At `71f1bc9` that call is
**deliberately removed** (function kept, unreferenced, with a comment explaining why). So the migration's
effect is *not* transient: at HEAD nothing repopulates, and caseloads stay empty until a human assigns.
Corollary: live master will keep growing the cross product on every boot — next live boot would add 20
rows (CH005/CH006 × 10 HPs each).

**(2) There are ZERO hand-made assignments in the live DB.** All 28 targeted rows have
`source ∈ {system, admission, admission-update}`; **no row anywhere has `source='caseload'` or `'manual'`**.
The 8 rows with `createdBy='centerhead'` (not 3) carry `source='admission'/'admission-update'` — they were
written by the **Admission Slip flow with centerhead logged in**, not by the Houseparent Module's
Assign Resident (`CaseLoad.tsx` posts `source:'caseload'`, `HouseparentAssignment.tsx` posts `'manual'`).
So the prompt's option (b) rationale — "human-made rows survive" — is based on a misreading of
`createdBy`. Latest such row: `ASN049` at `2026-09-25T23:45:24Z` = **09-26 07:45 +08:00**, ~80 min
before the snapshot.

**(3) `master` (live) has no source guard; `71f1bc9` adds it.** `MANUAL_ASSIGNMENT_SOURCES=['caseload','manual']`
+ a 403 for any other source on a houseparent-type row, and `ChildRecords.tsx` stops POSTing
`source:'admission-update'` (master:2369). So shipping closes the writer that is producing these rows.

**What each option actually does (per Houseparent account, from the live snapshot):**

| option | rows ended | residents left with ≥1 Houseparent | effect on HP accounts |
|---|---|---|---|
| (a) ship as-is | 28 | 0 (the 21 surviving `household` rows are **invisible** — `residentScope` filters `assignmentType='houseparent'`) | all **11** empty |
| (b) `AND (createdBy IS NULL OR createdBy IN ('system','seed'))` | 20 | 6 of 6 | U007/UHP01/UHP02 keep rows; **HP3–HP10** (8 pure-seed accounts) empty |
| (c) disable the migration | 0 | 6 of 6 | unchanged |

Two things the prompt did not surface: under **(b)** U007 ends up holding **5** residents, over the
`MAX_RESIDENTS_PER_HOUSEPARENT = 3` cap (the cap is only checked on create, so it does not error — the UI
just shows an over-cap caseload). And under **(a)** the Houseparent caseload filter also gates
**document** access, so emptying it locks a Houseparent out of filing TRI/incident/anecdotal for their
resident until re-assignment — that is the "breaks production" case, not a cosmetic one.

**Correction to the prompt's §3.1 aside:** `houseparentOnDuty`/`houseparentUserId` are absent on every
`/children` row, but that proves nothing — those columns live on `admissions`, and `server.js:553`
declares `houseparentOnDuty` in the `admissions` CREATE TABLE with `houseparentUserId` `ensureColumn`'d at
`:668`. The columns **do** exist. The fallback is nonetheless harmless to delete, because the admission
writer also creates an explicit `residentAssignments` row (ASN049, 80 min old) — so no live resident's
caseload depends on the slip path. Verify by the *table that holds the column*, not by a sibling endpoint.

**Reached for the decision, not yet answered.** Recommended **(b)**.

---

## PHASE C/D/E COMPLETE (2026-09-26, second session)

### §3.1 decision: option (b), and one earlier note corrected

Badek delegated it ("you decide what is best"). Shipped **(b)** — narrow the predicate to
`AND (createdBy IS NULL OR LOWER(TRIM(createdBy)) IN ('system','seed'))`.

**Correction to the table above:** `MAX_RESIDENTS_PER_HOUSEPARENT` is **15**, not 3
(`assignmentController.js:103`, confirmed live — every caseload row answers `maxCaseload: 15`).
So U007 ending up with 5 residents is well inside the cap; the "over-cap" caveat recorded above
was based on a wrong constant and does **not** apply.

**Live re-verification of (b)** against `/api/resident-assignments/caseload` + per-resident rows:

| | count |
|---|---|
| Active `houseparent` rows live | 28 |
| ended by (b) | 20 (all `source='system'`, `createdBy='system'`) |
| spared by (b) | 8 (`admission`/`admission-update`, `createdBy='centerhead'`) |
| residents still holding ≥1 Houseparent | **6 of 6** |
| `household` rows (untouched — the migration filters `assignmentType='houseparent'`) | 21 |

Per account after the migration: U007 **5 distinct residents**, UHP01 1, UHP02 1, UHP03–UHP10 0.
No resident is orphaned, so the "empties the caseload and locks Houseparents out of filing" failure
mode of (a) is avoided.

### Failure table worked to zero

Suite **1326 tests / 1320 pass / 6 fail** — all 6 environmental (5 `jwt-secret`, 1 `modal-stacking`).
Started at 76 fails. Test count rose 1323 → 1326 (3 new behavioural tests in
`tests/tri-signature-stamp.test.js`).

**Real regressions fixed (code), each mutation-proven:**

1. `admissionController.js` — the merge dropped `const BODY_MARKINGS = require('../config/bodyMarkings.json')`
   and the two derived constants, so `normalizeBodyMarkings` threw `ReferenceError` on every call and
   the create path referenced an out-of-scope identifier (a live 500). 7 tests.
2. `server.js` — the merge dropped `ensureColumnLength('violations', 'type', 500, 'VARCHAR(500) NOT NULL')`.
   `schema.sql` is never executed, so a deployed DB keeps `VARCHAR(100)` and logging a long-named
   violation 500s as "Database error occurred". 1 test.
3. `InterventionTracker.tsx` — the merge spliced **both** versions of the Form 08 gate fix: our
   `form8Section` helper (used only for the caption) and his inline gate (used for visibility). Same
   semantics, two sources of truth. Re-pointed the render at the helper. 3 tests.
4. `ChildDetail.tsx` — **`vite build` was RED**: `const isAbsconded` declared twice (merge splice).
   Neither the suite nor `node --check` can see this (they do not parse `.tsx`); the build is the gate.
   Build is green after removing the duplicate.
5. Two contradicting docblocks removed: `documentController.reopenInterventionForReassessment` (ours
   described reopening the tracker; his superseded it with a no-op) and `incidentReportController`'s
   Form 08 name block. Plus a stale tracker docblock claiming the backend reopens the rows.

**Superseded pins updated (tests), per §4/§5:** body-markings column list; Form 08 pre-printed names
(HIS: endorsement typed, `MARICOR C. NAVARRO, RSW` noted-by, psych-staff box); Houseparent matrix
`Violations: ['view','create']` + `Activities: ['view','export']` (his 2-line edit, §3.4 — also
`canAssignCaseLoadManager` now admits `socialworker`, which his controller *and* his SPA agree on);
`admission-houseparent-snapshot` (his `ChildRecords` deliberately dropped the `assignment?.userId`
fallback — our own `houseparent-assignment-identity` test already pinned that); `court-hearing-type`
(his Dashboard removed the Hearing Schedule card); `health-forms-and-document-filters` (his Health.tsx
added a `laboratoryResults` column and named the BMI constants); `violation-schedule-rule` (the rule is
now narrowed to the psych submission, not removed); `staff-signature-pad` (a fifth `psychStaff` box,
band 798–836 measured off the template with `C:/tmp/form08-template-runs.js`).

**Merge-splice test bug:** `reassessment-reopens-intervention.test.js` took HIS header (8 tests) and
appended OUR 4 tracker tests, but his header has no `const INCIDENT_MODAL` — our appended test
referenced it. Added the const.

### Phase D gates — all green

- `node --check` on all 26 changed `.js` files: **0 failures**.
- suite: **1326 / 1320 pass / 6 fail** (baseline).
- `vite build` to a fresh dir with `--emptyOutDir`: **green**; entry `index-*.js` 112.15 kB
  (33.28 kB gzip), inside the ≈142 kB budget.
- `tsc --noEmit`: **9 errors in 4 components** — the documented baseline (4 AssessmentDetail,
  3 ChildDetail implicit-any, 1 Assessments, 1 ChildRecords). None caused by this work.

### Phase E — live read-only verification (deployed pair)

- `POST /api/auth/login` `centerhead`/`centerhead123` → 200.
- 200: `/api/children`, `/api/store` (`{success,data}`, 103 kB), `/api/tri` (219 kB),
  `/api/tri/monitor`, `/api/violations`, `/api/alerts`, `/api/dashboard/schedule-summary`.
  `/api/store` and `/api/dashboard/...` are mounted **under `/api`** — the bare `/store` in the prompt
  is a 404 for that reason, not a defect.
- 404 is correct (no `GET /`) for `/api/admissions`, `/resident-assignments`, `/api/discharge-plans`,
  `/api/incident-reports`.
- Timezone contract intact: `/api/store` datetimes carry a zone; `readmissionDatetime`
  `2026-09-25T09:44:11.000Z`; `WALL_CLOCK_DATETIME_COLUMNS` is still only
  {`incidentDateTime`, `scheduledAt`} and `serverTime.js` is untouched by the merge.
- SPA `sch-path.vercel.app` → 200, `Last-Modified: Fri, 25 Sep 2026 15:48:42 GMT`
  (captured for the post-deploy comparison).
- Pre-deploy captures: `C:/tmp/PRE-caseload.json`, `C:/tmp/PRE-store.json`, `C:/tmp/ra-CH00*.json`.
- **Not possible read-only:** the prompt's "capture `/resident-assignments/my-residents` for a
  Houseparent" — that needs a Houseparent credential, and the live `socialworker` password is not the
  seed, so no real account was reset. `centerhead` is full-access, so its `my-residents` is not the
  scoped shape. Say so plainly rather than implying the capture was taken.

---

## PRE-PUSH RE-VERIFICATION (2026-09-26, third session — "is it safe to push?")

Re-ran every gate on the *current* working tree instead of quoting Phase D. Nothing pushed: branch
`merge/partner-2026-09-26` @ `71f1bc9`, local `master` = remote `master` = `24d6fa6`
(`git ls-remote origin master`). `master` **is an ancestor** of `71f1bc9` → a fast-forward is possible.

- **Tree vs `master`: 58 files changed, 0 deletions** — nothing from master is dropped at file level.
  30 modified + 3 untracked (`memory/2026-09-26.md`, `prompts/`, `tests/tri-signature-stamp.test.js`).
  `lf-fix.py` normalised **0** files → the 30 dirty files are all real content diffs, **no CRLF false
  positives** this time (unlike the ~22 the earlier session saw).
- The 11 files the session summary did not list (`Tri.tsx`, `triController.js`, `triReportPdf.js`,
  `schema.sql`, `App.tsx`, `dashboardController.js`, `violationGuideController.js`, and 4 TRI test
  files) are the **first session's** merge work, not stray edits. `git diff --stat master 71f1bc9` =
  47 files; the working tree is the merged tree plus both sessions' fixes.
- `node --check` on all **37** changed `.js` (vs master): **0 failures**.
- suite: **1326 / 1320 pass / 6 fail** — all 6 environmental: 5 `jwt-secret.test.js` (sandbox blocks
  `spawnSync`, child stdout is `''` so the regex cannot match) + 1 `modal-stacking.test.js:408`
  (`spawnSync … EBUSY`). Both are `testCodeFailure` from the harness, not the code.
- `vite build --outDir dist-verify-push --emptyOutDir` → **green**, entry `index-CF70xqrD.js`
  112.15 kB / 33.28 kB gzip. Hash identical to the Phase D build → the build is deterministic here.
  Artifact deleted afterwards (`dist-verify*/` is gitignored, so it never shows in status).
- `tsc --noEmit` → **9 errors / 4 components**, the documented baseline.
- `rbac.definition.json` and `bodyMarkings.json`: both copies **byte-identical**.

**Live baseline re-captured** (`centerhead`/`centerhead123`, read-only): `/api/health` 200;
`/api/store` 200 (103,257 B); `/api/children` 200 (6 residents); Vercel `index.html`
`Last-Modified: Fri, 25 Sep 2026 15:48:42 GMT`, entry chunk `/assets/index-D9QvwdHN.js`.
`/api/resident-assignments/caseload` → 200: **11 accounts, 27 assigned-resident rows, all 6 residents
covered** (U007 5, UHP01 3, UHP02 3, UHP03–UHP10 2 each; `maxCaseload` 15 on every row — confirms the
earlier 15-not-3 correction).

**§3.1 predicate as actually shipped** (`server.js:1252-1262`) is narrower than the option text — it
needs **both** an automatic source **and** a machine `createdBy`:
`AND source IN ('system','admission','admission-update') AND (createdBy IS NULL OR LOWER(TRIM(createdBy))
IN ('system','seed'))`. So the `createdBy` clause alone is not the guard; a row must satisfy both.
The docblock at `:1239-1247` calls `createdBy` "the second gate, and it is load-bearing".

**Residual, stated rather than papered over:** the snapshot's 28 rows / 20-ended / 8-spared split can
no longer be recomputed read-only. Live is now 27 rows and the live DB keeps changing because **master's
boot still calls `seedResidentAssignments()`** (the cross product), which `71f1bc9` removes. No endpoint
exposes per-row `source`/`createdBy` to a Center Head: `/api/resident-assignments/my-residents` → **403**
(Houseparent-scoped), `/history`, `/all`, `/api/assignments` → 404. So the exact affected-row count at
push time is knowable only from Railway's boot log line
(`Migration: ended N automatic Houseparent assignment(s)`), which is the first thing to read after
deploy. The invariant that actually matters — no resident drops to zero surviving Houseparent rows —
held at the snapshot and is what that log line should be checked against.

---

## SHIPPED — 2026-09-26, independent re-verification then push

Pushed as three commits on `master`, all fast-forwards (no force, no merge commit):

| sha | what |
|---|---|
| `fb8c1b2` | the partner re-integration + the audit fixes (34 files) |
| `efb0722` | index `education_records.createdAt` |
| `68736eb` | sort the Education list in the application |

`master` was an ancestor of `71f1bc9`, so `git push origin HEAD:master` fast-forwarded. Nothing was
checked out, so `core.autocrlf` never rewrote the tree; `lf-fix.py` reported 0 files to normalise.

### The one real bug, and the two-layer trap behind it

`GET /api/education-records` was **400 on live for every role**, and predated the merge (master has the
same `orderBy` and the same missing column). It was masked twice over:

1. `education_records` was provisioned before `createdAt`/`updatedAt` were in its `CREATE TABLE` body.
   `CREATE TABLE IF NOT EXISTS` is a no-op on an existing table, so nothing ever added them, and
   `ORDER BY createdAt DESC` failed with `ER_BAD_FIELD_ERROR` — which `errorHandler` masks to
   "Database error occurred" with the column unnamed. Fixed with a guarded `ensureColumn`.
2. **After that fix it still 400'd**, now `ER_OUT_OF_SORTMEMORY`. `files` holds the learner's uploads
   as JSON and live row `EDU001` measured **277 KB** of it against a 256 KB default
   `sort_buffer_size`. MySQL filesorts a row together with the columns it carries, so the payload is
   what breaks the sort. This was invisible until (1) was fixed: **an unknown column is rejected while
   the statement is prepared, so the sort is never reached.** Two errors on one statement, the second
   hidden behind the first.
3. An index on the sort column was **not sufficient** — with this few rows the optimiser still prefers
   a full scan plus a filesort. Verified live: the index deployed and the endpoint still 400'd. Fixed
   properly with `RESOURCES[x].sortInApplication: true`, which makes `baseController.getAll` issue no
   `ORDER BY` at all and sort in process. Deterministic, no plan to be declined.

Lesson worth keeping: when a fix makes an endpoint fail *differently*, that is progress — read the new
error rather than assuming the fix failed.

### `isSnapshot` — a page-killing crash, found by accident

Chasing a different problem, a synthetic Houseparent session crashed `/violations` with
`Cannot read properties of undefined (reading 'some')`. Root cause: `isSnapshot()` returned true for
**any object carrying a `fullAccess` key**, and `AuthContext` stores `fullAccess` on the session user
next to a null `access`. So the user object was mistaken for a resolved snapshot and the next read,
`snapshot.modules.some(...)`, threw. Reachable for any session restored from localStorage with no
`access` field — every role that is not a full-access role. A real snapshot always carries `modules`,
so the guard now requires `Array.isArray(modules)`.

**The near-miss matters more than the bug**: my first fixture set `access: null` while keeping
`fullAccess: false`, and I nearly wrote it off as a fixture artifact. It *was* unrealistic for a
session created by the current login (which always ships `access`) — but only because the login
payload happens to supply it. Building the fixture from the server's own `buildAccessSnapshot()` +
`listAccessibleMenus()` removed the guesswork, and the `access`-less case was then kept as a real
regression case. **Derive fixtures from the server's own code; do not hand-write a session shape.**

### Verification that actually exercised things

- **All 138 registered GET routes probed against live.** Only `/education-records` failed. After the
  fix: 94 OK, 0 real findings (the one flagged item is the SSE stream, correctly non-JSON).
- **Merged bundle driven in a browser against the live API**, 17 routes, **all non-GET aborted at the
  network layer** — 0 write attempts, which is also the proof the drive was read-only.
- **Two-branch test for the Violations fix**: failure → failure copy, success-and-empty → the original
  copy. A fix that merely replaced the message would pass one and fail the other.
- **The deployed Vercel artifact driven with the crashing session** — the only way to prove the fix is
  live rather than merely committed, since a local build's hash never matches Vercel's.
- Mutation-proved all three new guards; each restored from bytes captured at apply time (never
  `git checkout --`), sha256 verified.
- Suite 1336 / 1330 pass / 6 fail — the 6 are the documented sandbox baseline (5 `jwt-secret`,
  1 `modal-stacking`).

### Corrections to earlier notes in this file

- "**6 tests fail here, identically at HEAD**" — still true, and now confirmed to survive three more
  commits unchanged.
- `contract-audit` Section B (50 columns in `expectedSchema` but not in `constants.js`) is **verified
  by design**, not merely labelled so. Each non-generic column was traced to its mechanism: `mapRow`
  is an exposure allowlist, `phaseController.js:109` reads `violationCount`/`demotionRecommended` with
  a raw SELECT and attaches them as a computed `violationBlock` after `mapRow`,
  `quarterlyProgressReportController` uses its own `mapReport` (`{...row}`), `users.password` is
  deleted by `/store`, `documents.fileData` is omitted from its explicit column list, and
  `violations.psychVerification` has **zero** frontend reads and is stripped from `req.body` at
  `violationController.js:1244`.
- The `dswd-format` route is **pre-existing dead code**, identical at master: the URL appears once in
  `frontend/src` and the route was never registered. Reported, not touched — out of scope for a merge
  and it is an exported-but-unconsumed context method.

## Repo state + MEMORY.md cleanup (11:20)

Asked "what happens to the workflow when my partner edits the system". Grounded the answer in the repo
rather than guessing:

- `HEAD` is on `merge/partner-2026-09-26` (`6793b2d`), **1 commit ahead of `origin/master`** and 0
  behind. `master` and `origin/master` are both `68736eb`. That one extra commit is the memory note —
  it exists only on this side branch and is unpushed. Local branches: `backup/pre-partner-merge`,
  `master`, `merge/partner-2026-09-25`, `merge/partner-2026-09-26`.
- Partner chain is 89 commits from `e9b6572` (baseline); fork point from pre-merge master was
  `e1cb775`. Their whole delta is 11 files, all additive: `expectedSchema.json` (+4504),
  `schemaSync.js` (+112), `polyfills.ts` (+34), `errorHandler.js` (±55), `violationController.js`
  (+40), `routes/index.js` (+27), `lazyComponent.tsx` (+27), `server.js` (+15), `App.tsx` (+7),
  `pdf.worker.mjs` (+3), `main.tsx` (+2).
- Their tree = 542 tracked files vs 566 on pre-merge master. The 25 missing ones are listed in the
  transcript; all are audit-era files (`isoTimestamps.js`, `alertStream.js`, `serverTime.js`,
  `bodyMarkings.json`, `admissionSlipMarkings.ts`, 21 tests). Confirms their working copy predates the
  audit pass — that is the mechanical reason `fb8c1b2` needed a manual re-integration.
- **`errorHandler` behaviour changed and my standing rule was stale.** It no longer masks `ER_*`:
  returns **400** with `code` + 200 chars of `sqlMessage`, and four schema codes now trigger an
  automatic `syncSchema(pool)` ALTER on the live DB from an ordinary request (60 s rate limit,
  single-flight, 409 "try again"). Also noted: the code comment claims values are never included, but
  `ER_TRUNCATED_WRONG_VALUE_FOR_FIELD`'s `sqlMessage` embeds the offending value — so the comment is
  wrong, low severity since the value is user-supplied.
- `utils/schemaSync.js` is a **third** schema mechanism alongside `runMigrations()` and the lazy
  `CREATE TABLE`s. Invoked from `server.js:2888`, `routes/index.js:57`
  (`GET /api/health/schema`, centerhead/admin, `?fix=1`), and `errorHandler.js:53`.
- MEMORY.md had grown to 11,113 chars and was being truncated on load. Rewrote it to 3,000 chars —
  kept only the never-get-this-wrong rules — and moved the build/deploy, tooling, git, schema-repair
  and endpoint-map detail into a new **`sch-path-repo-ops`** skill so nothing was lost.

## Live verification pass — "is the system 100% good?" (11:26)

Probed the live pair read-only, no mutations. **Answer: no — one real (latent) defect found.**

Verified good:

- `GET /api/health` 200; `GET /api/health/db` 200 `{"database":"connected","latencyMs":1}`.
- Unauthenticated `GET /api/children` → **401** (global `authenticate` intact).
- `GET /api/health/schema` → `{"ok":true,"missingTables":[],"missingColumns":[],"enumsMissingValues":[]}`
  — zero drift against `expectedSchema.json`.
- 23 endpoints returned 200, including `/store` (19 tables, 107 KB), `/children`, `/alerts`,
  `/violations`, `/tri` (352 KB), `/tri/monitor`, `/tri/summary`, all four `/education-*`,
  `/documents`, `/quarterly-progress-reports`, `/phases`, `/health-records`, `/anecdotal-reports`
  (42 KB), `/violation-guide` (172 KB), `/access-requests`, `/dashboard/schedule-summary`.
- `children` rows carry `abscondedAt`/`abscondedBy` on 6/6 → the boot migration applied.
- Vercel: production deployment `READY` / `PROMOTED` at `68736eb1e1a5679b1502797b876f46e0118ab20f`
  = master. Backend is ≥ `68736eb` (proved behaviourally: `/api/health/schema` exists → ≥1132590, and
  `/api/education-records` returns 200 on the 277 KB row, which needs `sortInApplication` from
  68736eb). Both halves in step.
- Every 404 seen was a legitimate route shape, checked against the routers: `/api/admissions`,
  `/api/discharge-plans` (only `/resident/:id`, `/tri/:id/recommendation`), `/api/resident-assignments`
  (mounted but `assignmentRoutes.js` has no `router.get`), `/api/activity-evaluations`, and
  `/api/court-records` (the real mount is camelCase `/api/courtRecords`). My two other 404s
  (`/api/notifications`, `/api/intervention-schedules`) are paths that do not exist in the codebase.

**Defect found — `/store` silently drops `education_records`:**

- `/api/education-records` → 200, `count: 1` (row `EDU001`, 277,761 bytes; the wide `files` blob).
- `/store` → `educationRecords: []`.
- Cause: `routes/index.js:276` builds its own `SELECT * FROM \`education_records\` ORDER BY createdAt
  DESC` and **never consults `sortInApplication`** (`constants.js:163`, added by `68736eb` for exactly
  this row). The filesort over the 277 KB row exceeds `sort_buffer_size`; the per-table `catch` at
  line 308 sets `data.educationRecords = []` and only `console.error`s.
- Proof it is the catch and not a guard: the caller was `centerhead`, which `EDUCATION_ROLES` admits,
  and `STORE_MODULE_BY_RESOURCE` has no `educationRecords` key — so neither guard can produce `[]`.
- Severity: **latent**. `grep educationRecords frontend/src` → zero hits; `Education.tsx:1015` and
  `Dashboard.tsx:1139` both call `/education-records` directly. No user-visible break today.
- The same shape applies to `education_school_visits` (`fileData` LONGTEXT) and
  `education_monthly_reports` (`reportData`) — both `[]` in `/store` and 0 rows on their own endpoints,
  so empty and swallowed cannot be told apart there.
- Fix not applied — out of scope for a status question, and it means a push to master = production.
  The fix is to route `/store` through the same sort path (or skip MySQL `ORDER BY` for resources with
  `sortInApplication`).

Risks noted, not defects: the partner's `errorHandler` → `syncSchema` auto-ALTER now lets an ordinary
request change production schema (additive, 60 s rate limit, 409); and `ER_*` responses carry `code` +
200 chars of `sqlMessage`, contradicting the in-code claim that values are excluded
(`ER_TRUNCATED_WRONG_VALUE_FOR_FIELD` embeds the offending value).

Could not verify: Railway's exact commit sha or server logs (no Railway token on this machine — the
backend commit is inferred from behaviour, not read); anything requiring a browser (no UI, login or
render check); writes (GETs only); the 6 known local test failures (unchanged, sandbox-related).

Technique added to the `sch-path-deploy-verify` skill: `/store` vs the resource's own endpoint as a
swallowed-error detector, plus the three health endpoints and the "verify a 404 against the router
before reporting it" rule.

## "Where do I check a change?" — settled (11:33)

Checked rather than assumed, and the answer is forced, not a preference:

- **There is no local database at all.** No `mysql` client, no `mysqld`, nothing listening on 3306.
  `backend/.env` reads `DB_HOST=localhost`, `DB_PORT=3306`, `DB_USER=<4 chars>`, **empty
  `DB_PASSWORD`**, `DB_NAME=sch_path_db`. So the local backend boots and then fails every DB call —
  localhost cannot verify a single endpoint, RBAC check or list view.
- Local's only real job is the **compile gate**: `frontend: npm run build` (the only thing that sees a
  `.tsx` parse error) and `backend: npm test` (the pinned tests).
- **Verification happens on the deployment.** Live API with the cached token at `C:/tmp/.sch-token`,
  plus the live URL in a browser.
- New option for UI iteration: `frontend/.env.local` (gitignored; `frontend/.env` is untracked too) with
  `VITE_API_URL=https://sch-path-production.up.railway.app/api` gives hot-reload against the **real**
  database. CORS permits it — see below. Reads only; writes hit production data.
- **CORS is wildcard in production.** `CORS_ORIGIN` is unset on Railway, so `server.js:89` falls
  through to `cors({})` and every response carries `access-control-allow-origin: *`. The allow-list the
  code comment describes is written but not in force. Low severity (auth is a bearer token in
  localStorage, not a cookie, so a wildcard origin does not by itself hand anyone a session), but it is
  the difference between the documented posture and the real one.

## Fixed the /store wide-row trap (11:41)

`f2d8363` — `baseController` now exports `sortRows`, and the `/store` loop honours `sortInApplication`
instead of always handing the ORDER BY to MySQL. Backend-only change; no frontend build needed.

`tests/store-wide-row-sort.test.js` is the pin, and its pool stub **throws ER_OUT_OF_SORTMEMORY when the
statement carries an ORDER BY**, so it reproduces the live failure rather than describing it. Verified it
genuinely fails on the pre-fix query by neutering the block, confirming subtests 2 and 3 go `not ok`,
then restoring from bytes captured before the swap (sha256 verified). The fourth subtest is the control:
`education_school_visits` must keep its SQL ordering, so a blanket "drop every ORDER BY" change cannot
pass. Full suite: **1334 pass / 6 fail**, the same six known failures (5 `jwt-secret`, 1 dialog-guard).

Still **not pushed**. HEAD is `merge/partner-2026-09-26`, now 3 commits ahead of `master`; only `master`
deploys. Lesson worth keeping: fixes land on side branches here and the deploy is a separate
fast-forward, so "committed" and "live" are two different states.




