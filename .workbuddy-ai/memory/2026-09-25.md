# 2026-09-25 — SCH-PATH

## The deploy is still stuck, ~17 hours on

`/api/health/db` returns 200, so the image is at least `88ea2ce` (18:00). But
nurse and educator still hold the drifted grants
(`["Dashboard","Activities","Documents","Health","Reports"]` and
`["Dashboard","Documents","Activities","Education"]`), which means the reconciler
from `cda48f5` (18:09) has **still never run**. Production has not advanced past
18:00 in seventeen hours.

**Ruled out my own code as the build breaker** — this was the thing worth
checking before blaming the platform again:

- `node --check` clean on every file I touched (`server.js`, `seedDatabase.js`,
  `accessDefaults.js`, `rbac.js`, `routes/index.js`, `reportRoutes.js`,
  `userController.js`).
- `require('./src/server.js')` imports and prints `✅ API routes mounted`, then
  fails only on the expected `ECONNREFUSED localhost:3306`. So the boot chain
  loads; nothing in my commits can fail the build.

No `railway` CLI, no `~/.railway`, no `RAILWAY_TOKEN` in the environment, and
`backend/.env` holds only local DB creds plus `JWT_SECRET` (gitignored, values
not read). So **the deploy status cannot be read from outside** — it needs the
Railway dashboard.

## The live roster changed while I was away

19 accounts now, was 18. A new `Nurse-ian` (role `nurse`) holds
`["Dashboard","Child Records","Documents","Health"]`, and `socialworker` went
from `[]` to the canonical nine. Someone is editing accounts through the live UI.

Worth noting how to read `Nurse-ian`'s ordering: `Documents` before `Health`
matches `MODULE_ORDER`, whereas the frontend default lists `Health` first. So
that row was canonicalized by the **backend** — it came in through the API, not
from the frontend default list. Useful tell.

Also: `socialworker` being non-empty is **not** evidence the reconciler ran. The
reconciler clears; it never fills. A filled socialworker means a human saved it.

## Read surface re-verified — clean

- `sweep3.js`: 55 static GET routes → **54 OK, 0 server errors**. The one 403
  (`/api/resident-assignments/my-residents`) is correct: `centerhead` is not a
  Houseparent.
- `sweep2.js`: 62 parameterised routes with real IDs → **0 server errors**, 3
  expected 403s.

117 endpoints, zero 5xx. `/api/reports/daily-data` returns 200, so the
route-shadowing fix (`8881406`) is confirmed live in production.

## Runtime note

The managed Node runtime moved from `22.22.2-2` to **`22.22.2-3`**. Scripts that
hardcode the old path now fail with a bare "No such file or directory" that looks
like a script bug. Check `binaries/node/versions/` before debugging anything else.

## Still open

- **Deploy blocker** — needs the Railway Deployments tab. Everything pushed since
  18:09 is queued and unbuilt.
- **Write paths have still never been exercised against live data.** Reads are
  now verified thoroughly; writes are not. Doing so safely means driving the
  controllers against a stub pool locally, not touching production.
- Item 9 ("Anecdotal") still has no recorded decision.
- The reconciler/migration double-write oscillation is still in place.
